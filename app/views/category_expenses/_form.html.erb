<%= form_with(model: category_expense) do |form| %>
  <% if category_expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_expense.errors.count, "error") %> prohibited this category_expense from being saved:</h2>

      <ul>
        <% category_expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :expense_id, style: "display: block" %>
    <%= form.text_field :expense_id %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
